import React from 'react'
// import { Button, Form } from 'semantic-ui-react'

export default function SignupForm() {
  const isLearner = true
  const isOTS = true
  const isLoading = false

  return (
    <section>
      {/* {isLoading && <div>Loading.. </div>}
      <section>
        <Button>Coach</Button>
        <Button>Learner</Button>
      </section>

      <section>
        <Form>
          <Form.Field>
            <input placeholder='Your name' />
          </Form.Field>
          <Form.Field>
            <input placeholder='What do you want to learn/coach?' />
          </Form.Field>
          {isLearner && (
            <Form.Field>
              <input placeholder='Do you have a project idea?' />
            </Form.Field>
          )}
          <Form.Field>
            <input placeholder='Day per week' />
          </Form.Field>
          <Form.Field>
            <input placeholder='Time frame (morning/afternoon/evening)' />
          </Form.Field>
          <Form.Field>
            <input placeholder='Interested to have a learn group?' />
          </Form.Field>
          {isOTS && (
            <Form.Field>
              <input placeholder='How do you know about OTS?' />
            </Form.Field>
          )}
          <Button type='submit'>Submit</Button>
        </Form>
      </section> */}
    </section>
  )
}

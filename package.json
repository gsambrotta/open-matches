{
  "name": "open-matches-backend",
  "version": "1.0.0",
  "description": "open matches project",
  "scripts": {
    "build-client": "cd client && npm run build",
    "start-server": "cd server && npm start",
    "start": "npm run build-client && npm run start-server ",
    "dev-client": "cd client && npm start",
    "dev": "concurrently --kill-others-on-fail --names \"🔙🔚,📱\" \"npm:start-server\"  \"npm:dev-client\" "
  },
  "keywords": [
    "matches",
    "open",
    "source"
  ],
  "author": "designbygio",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
